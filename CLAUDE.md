AI 코딩 에이전트 작업 규칙

이 문서는 AI 코딩 에이전트와 프로젝트 참여자가 소스 코드를 분석·수정·검증·배포할 때 반드시 따라야 하는 작업 규칙을 정의한다.

📌 Claude Code 사용 시: 이 문서를 프로젝트 루트에 CLAUDE.md로 저장하면 Claude Code가 세션 시작 시 자동으로 읽어들인다. 자세한 연동 방법은 15장 참고.

1. 기본 원칙
1.1 기존 기능 보존
사용자의 명시적인 요청 없이 기존 기능을 삭제하거나 비활성화하지 않는다.
수정 대상과 직접 관련이 없어 보이는 코드도 임의로 정리하거나 제거하지 않는다.
기존 API, 화면, 설정 항목, 실행 스크립트와의 호환성을 가능한 한 유지한다.
기존 기능을 변경해야 하는 경우에는 변경 이유와 영향 범위를 먼저 분석한다.
기능 대체가 필요한 경우 기존 기능의 동작을 보존하면서 단계적으로 교체한다.
1.2 수정 전 분석

코드를 수정하기 전에 다음 항목을 확인한다.

프로젝트 디렉터리 구조
수정 대상 파일
수정 대상 파일을 참조하거나 호출하는 파일
관련 API 요청 및 응답 구조
관련 JSON 설정 파일
환경 변수 및 백엔드 설정
PowerShell 및 배치 실행 스크립트
기존 버전 이력과 문서
수정으로 영향을 받을 수 있는 기존 기능

관련 파일을 일부만 확인한 상태에서 바로 수정하지 않는다. 프런트엔드, 백엔드, 설정 파일, 실행 스크립트가 연결된 기능은 전체 호출 흐름을 확인한 후 수정한다.

2. 실행 환경
2.1 Windows 기준

이 프로젝트의 기본 실행 환경은 Windows이다.

명령어는 Windows PowerShell을 기준으로 작성한다.
자동 실행 및 배포 스크립트는 .bat 또는 .ps1 형식을 우선 사용한다.
Linux 전용 명령어를 기본 사용 방법으로 작성하지 않는다.
경로 처리 시 Windows 경로 구분자와 공백이 포함된 경로를 고려한다.
실행 파일과 디렉터리 경로는 가능한 한 따옴표로 감싼다.

예시:

powershell
Set-Location "C:\Project\RIC"
npm install
npm run build

배치 파일에서는 오류 발생 시 이를 감지할 수 있도록 종료 코드를 확인한다.

bat
@echo off
call npm install
if errorlevel 1 (
    echo [ERROR] npm install failed.
    exit /b 1
)
2.2 실행 스크립트 유지
기존 .bat 및 .ps1 파일은 임의로 삭제하지 않는다.
실행 방식이 변경되면 관련 실행 스크립트도 함께 수정한다.
백엔드 전용 실행과 브라우저 자동 실행이 구분되어 있다면 기존 구분을 유지한다.
스크립트 수정 후에는 실제 Windows 명령 문법을 검사한다.
2.3 인코딩 및 줄바꿈 (신규)

Windows + 한글 환경에서 가장 흔하게 발생하는 문제이므로 별도로 관리한다.

소스 파일은 UTF-8 (BOM 없음) 을 기본 인코딩으로 사용한다. 단, .bat 파일은 CP949(시스템 기본 로캘)로 저장해야 한글 출력이 깨지지 않는 경우가 많으므로 프로젝트 내에서 통일된 규칙을 정해 docs/README.md에 명시한다.
PowerShell 콘솔에서 한글 출력이 깨질 경우 스크립트 상단에 다음을 추가하는 것을 고려한다.
powershell
  [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
Git 저장소에서 줄바꿈 문제를 방지하기 위해 .gitattributes로 정책을 명시한다.
  * text=auto eol=lf
  *.bat text eol=crlf
  *.ps1 text eol=crlf
팀원 간 core.autocrlf 설정이 다르면 동일 파일이 매번 diff에 잡히는 문제가 생기므로, .gitattributes로 통일하고 개인 Git 전역 설정에 의존하지 않는다.
3. 보안 및 API 키 관리
3.1 프런트엔드 저장 금지

API 키, 비밀 키, 인증 토큰 및 비밀번호는 프런트엔드 코드에 저장하지 않는다.

다음 위치에 API 키를 직접 작성하지 않는다.

JavaScript 및 TypeScript 프런트엔드 파일
HTML 파일
브라우저에서 내려받을 수 있는 JSON 파일
빌드 결과물
클라이언트 환경 변수
Git 저장소에 포함되는 공개 설정 파일
3.2 백엔드 환경 설정 사용

외부 API 호출은 백엔드에서 수행한다.

API 키는 다음과 같은 백엔드 전용 환경 설정을 사용한다.

EXTERNAL_API_KEY=replace_with_real_key

백엔드에서는 환경 변수를 통해 값을 읽는다.

js
const apiKey = process.env.EXTERNAL_API_KEY;

if (!apiKey) {
  throw new Error("EXTERNAL_API_KEY is not configured.");
}

다음 파일은 실제 비밀값을 포함하지 않는다.

.env.example
샘플 JSON 설정
docs/README.md
HISTORY.md

.env.example에는 변수 이름과 설명만 작성한다.

비밀값 판단 기준 (신규): "이 값이 외부에 노출되면 인증 우회, 과금, 개인정보 접근이 가능한가?"에 해당하면 .env(백엔드 전용)로, 단순 서비스 주소·포트처럼 노출되어도 직접적인 피해가 없는 값이면 4장의 JSON 설정으로 분류한다. 내부망 전용 IP처럼 애매한 경우, 외부 공개 저장소에 올라갈 가능성이 있는 프로젝트라면 보수적으로 .env 쪽으로 분류한다.

3.3 응답 데이터 보호

백엔드 오류 응답이나 로그에 다음 정보가 노출되지 않도록 한다.

API 키
인증 토큰
사용자 비밀번호
전체 인증 헤더
민감한 서버 환경 변수
4. 서버 IP 및 포트 관리
4.1 JSON 설정 파일 사용

서버 IP, 호스트, 포트 및 서비스 URL은 소스 코드에 직접 고정하지 않고 JSON 설정 파일에서 관리한다.

예시:

json
{
  "server": {
    "host": "0.0.0.0",
    "publicIp": "192.168.0.199"
  },
  "ports": {
    "main": 8090,
    "hlsConverter": 8080,
    "reportDashboard": 3100,
    "weatherDashboard": 8200
  }
}
4.2 하드코딩 금지

다음과 같은 주소를 여러 소스 파일에 직접 작성하지 않는다.

http://localhost:8080
http://127.0.0.1:8090
http://192.168.0.199:3100

서비스 URL은 JSON 설정값으로 조합한다.

js
const baseUrl = `http://${config.server.publicIp}:${config.ports.hlsConverter}`;
4.3 주소 조합 검증

URL을 조합할 때 다음 문제를 확인한다.

이중 슬래시
누락된 프로토콜
잘못된 포트
localhost로 인한 원격 접속 실패
0.0.0.0을 클라이언트 접속 주소로 사용하는 문제
프런트엔드와 백엔드 설정 불일치

0.0.0.0은 서버의 바인딩 주소로 사용할 수 있지만 브라우저가 접속하는 공개 주소로 사용하지 않는다.

5. 코드 수정 절차

모든 수정 작업은 다음 순서를 따른다.

5.1 분석
사용자 요구사항을 정리한다.
관련 파일을 모두 확인한다.
기존 동작을 파악한다.
변경 범위와 영향 범위를 확인한다.
기존 기능 손상 가능성을 검토한다.
5.2 수정
요구사항에 필요한 최소 범위를 수정한다.
기존 기능과 인터페이스를 유지한다.
설정값은 가능한 한 JSON 또는 백엔드 환경 변수로 분리한다.
오류 처리와 사용자 메시지를 함께 검토한다.
운영 환경이 Windows임을 고려한다.
5.3 검증
문법 검사
빌드 검사
실행 검사
관련 API 검사
기존 주요 기능 회귀 검사
실행 스크립트 검사
배포 대상 파일 검사
5.4 실패 시 처리

검증 실패 상태로 작업을 완료하지 않는다.

검증이 실패하면 다음 순서로 처리한다.

오류 메시지와 로그를 확인한다.
실패 원인을 분석한다.
원인을 수정한다.
동일 검증을 다시 수행한다.
관련 회귀 검사를 다시 수행한다.
모든 필수 검증이 통과한 후 결과물을 준비한다.

검증 실패 사실만 보고하고 수정하지 않은 상태로 배포 ZIP을 만들지 않는다.

6. 문법 검사 및 실행 검사

프로젝트에서 사용하는 기술에 맞는 검사를 수행한다.

6.1 JavaScript 및 Node.js

가능한 경우 다음 검사를 수행한다.

powershell
npm run lint
npm run build
npm test

별도의 스크립트가 없다면 최소한 JavaScript 문법을 검사한다.

powershell
node --check ".\path\to\file.js"

JSON 파일은 파싱 검사를 수행한다.

powershell
Get-Content ".\config\settings.json" -Raw | ConvertFrom-Json | Out-Null
6.2 Python
powershell
python -m compileall .

필요한 경우 서버를 실행해 주요 API의 응답을 확인한다.

6.3 PowerShell
powershell
$errors = $null
[System.Management.Automation.Language.Parser]::ParseFile(
    ".\script.ps1",
    [ref]$null,
    [ref]$errors
) | Out-Null

$errors
6.4 배치 파일

배치 파일은 별도 정적 검사만으로 충분하지 않을 수 있으므로 테스트 환경에서 직접 실행한다.

확인 항목:

경로 이동
환경 변수 설정
실행 파일 존재 여부
오류 코드 처리
서버 실행 여부
브라우저 실행 여부
종료 시 프로세스 정리 여부
6.5 실행 검사

문법 검사만 통과했다고 작업을 완료한 것으로 판단하지 않는다.

가능한 범위에서 다음 항목을 실제로 확인한다.

서버가 정상적으로 시작되는지
지정된 포트에서 대기하는지
주요 API가 정상 응답하는지
프런트엔드가 백엔드에 연결되는지
JSON 설정 변경이 실제 실행에 반영되는지
기존 핵심 화면과 기능이 유지되는지
6.6 테스트 커버리지 기준 (신규)
새로 추가하는 함수·API 엔드포인트에는 최소 1개 이상의 정상 케이스 테스트를 작성한다.
버그 수정 시에는 해당 버그를 재현하는 테스트를 먼저 추가한 뒤 수정하는 것을 원칙으로 한다(회귀 방지).
프로젝트에 테스트 프레임워크가 아직 없다면, 최소한 수동 검증 절차를 HISTORY.md의 "검증" 항목에 구체적으로 기록한다(예: "POST /api/report 200 응답 확인, 잘못된 파라미터 시 400 응답 확인").
7. 승인 문서 보호 정책

ℹ️ 이 장은 전자결재/승인 워크플로우가 있는 프로젝트에만 해당한다. 해당 기능이 없는 프로젝트라면 이 장은 적용하지 않는다.

7.1 승인 완료 문서

최종 승인 또는 최종 결재가 완료된 문서는 일반 사용자가 다음 작업을 할 수 없도록 한다.

내용 수정
문서 삭제
승인 취소
결재 정보 변경
승인 상태 이전 단계로 복원
첨부 파일 임의 교체

이 제한은 화면에서 버튼만 숨기는 방식으로 구현하지 않는다. 백엔드 API에서도 권한과 문서 상태를 반드시 검사한다.

예시 정책:

문서 상태가 FINAL_APPROVED인 경우:
- 일반 수정 API 거부
- 일반 삭제 API 거부
- 일반 승인 취소 API 거부
7.2 백엔드 검증

프런트엔드 조작이나 직접 API 호출로 제한을 우회할 수 없도록 백엔드에서 검증한다.

js
if (document.status === "FINAL_APPROVED" && !user.isAdmin) {
  return response.status(403).json({
    ok: false,
    message: "최종 승인된 문서는 수정하거나 삭제할 수 없습니다."
  });
}
7.3 승인 문서 오류 처리

승인 완료 문서에서 오류가 발견되어도 기존 문서를 일반 삭제하지 않는다.

관리자 권한으로 다음 절차를 사용한다.

기존 문서를 무효 처리한다.
무효 처리 사유를 기록한다.
기존 문서는 감사 이력을 위해 보존한다.
정정 문서를 새로 작성한다.
정정 문서는 정상 승인 절차를 다시 진행한다.
기존 문서와 정정 문서의 연결 관계를 기록한다.
7.4 감사 이력

다음 항목은 가능한 한 감사 이력으로 남긴다.

문서 작성 / 수정 / 승인 요청 / 반려 / 재상신 / 중간 승인 / 최종 승인
관리자 무효 처리 / 정정 문서 생성
작업자 / 작업 일시 / 변경 사유
8. Git 및 버전 관리 (신규)
8.1 커밋 규칙
커밋 메시지는 변경 목적이 드러나도록 작성한다. 예: fix: 리포트 대시보드 포트 하드코딩 제거, feat: 최종 승인 문서 삭제 차단 API 추가
하나의 커밋에는 관련 없는 변경사항을 섞지 않는다(설정 변경과 기능 추가를 분리).
main/master 브랜치에 직접 강제 push(git push -f)하지 않는다.
8.2 민감정보가 실수로 커밋된 경우
즉시 해당 키/토큰을 서비스 제공처에서 폐기(rotate)한다. 히스토리에서 지워도 이미 노출된 값은 무효화해야 한다.
git filter-repo 또는 BFG Repo-Cleaner로 이력에서 제거한다.
.gitignore에 해당 파일 패턴을 추가해 재발을 방지한다.
HISTORY.md에 사고 발생 및 조치 사실을 기록한다(실제 키 값은 기록하지 않는다).
8.3 .gitignore 최소 항목
node_modules/
.env
*.log
__pycache__/
.vscode/
.idea/
9. 배포 ZIP 작성 규칙

배포 ZIP에는 실행과 설치에 필요한 소스 코드, 설정 예제, 문서 및 스크립트만 포함한다.

다음 항목은 배포 ZIP에서 제외한다.

node_modules/
__pycache__/
*.pyc
*.pyo
*.log
logs/
log/
tmp/
temp/
.cache/
coverage/
.vscode/
.idea/
.git/
.env
npm-debug.log*
yarn-debug.log*
yarn-error.log*
Thumbs.db
.DS_Store

프로젝트 특성상 실행 중 생성되는 다음 파일도 검토 후 제외한다.

HLS 세그먼트
변환 중간 파일
캐시 데이터
테스트 출력물
임시 이미지
런타임 PID 파일
디버그 덤프
사용자 업로드 테스트 파일

단, 실행에 반드시 필요한 빈 디렉터리는 .gitkeep 등의 방식으로 구조를 유지할 수 있다.

10. 배포 전 확인

ZIP 생성 전에 다음 항목을 확인한다.

node_modules가 제외되었는지
실제 .env 파일이 제외되었는지
API 키와 비밀번호가 포함되지 않았는지
로그와 임시 파일이 제외되었는지
JSON 설정 예제가 포함되었는지
설치 및 실행 문서가 최신인지
실행 배치 파일이 포함되었는지
필요한 의존성 목록이 포함되었는지
HISTORY.md가 갱신되었는지
변경된 사용 방법이 docs/README.md에 반영되었는지

ZIP 생성 후 압축 파일의 내부 목록을 다시 확인한다.

11. 변경 이력 관리

모든 버전 수정 사항은 프로젝트 루트의 HISTORY.md에 기록한다.

기록 항목:

작업 날짜
버전 또는 배포 디렉터리명
수정 목적
변경된 주요 파일
추가된 기능
수정된 오류
설정 변경 사항
호환성 또는 주의사항
검증 결과

예시:

## 2026-08-03 — RIC_V78_02_Config_Security

### 변경 사항
- API 키를 프런트엔드에서 제거하고 백엔드 환경 변수로 이전
- 서버 IP와 포트를 JSON 설정 파일로 통합
- 최종 승인 문서의 일반 수정 및 삭제 차단
- 배포 ZIP에서 로그와 임시 파일 제외

### 검증
- JSON 문법 검사 통과
- Node.js 문법 검사 통과
- 백엔드 실행 검사 통과
- 승인 완료 문서 수정 및 삭제 차단 확인

기존 이력은 삭제하거나 덮어쓰지 않고 최신 내용을 추가한다.

12. 사용 문서 관리

설치 방법, 실행 방법, 설정 방법 또는 사용 방법이 변경되면 docs/README.md를 함께 수정한다.

다음 변경은 반드시 문서에 반영한다.

실행 명령 변경
배치 파일 이름 변경
포트 변경
서버 IP 설정 방법 변경
JSON 설정 구조 변경
환경 변수 추가 또는 변경
API 키 설정 방법 변경
의존성 설치 방법 변경
화면 사용 방법 변경
승인 절차 변경
배포 및 복구 방법 변경

문서에 실제 API 키, 비밀번호 또는 운영 서버의 민감한 정보를 기록하지 않는다.

13. 완료 조건

다음 조건을 모두 만족해야 작업이 완료된 것으로 판단한다.

관련 파일 분석 완료
요구사항 반영 완료
기존 기능 임의 삭제 없음
API 키의 프런트엔드 노출 없음
서버 IP와 포트의 JSON 설정 관리 확인
문법 검사 통과
빌드 또는 실행 검사 통과
실패 항목 수정 후 재검증 완료
승인 완료 문서 보호 정책 확인 (해당 프로젝트인 경우)
HISTORY.md 갱신
필요시 docs/README.md 갱신
배포 ZIP 불필요 파일 제외 확인
최종 결과물에 비밀정보가 없는지 확인
14. 금지 사항

다음 작업을 하지 않는다.

관련 파일을 분석하지 않고 수정
사용자 요청 없이 기존 기능 삭제
프런트엔드에 API 키 저장
서버 IP 및 포트를 여러 파일에 하드코딩
검증 실패 상태로 결과물 배포
승인 완료 문서를 일반 사용자가 수정 또는 삭제하도록 허용
실제 .env 파일을 배포 ZIP에 포함
node_modules, 로그 또는 임시 파일을 배포 ZIP에 포함
변경 이력 및 사용 문서 갱신 누락
오류를 숨기거나 검증하지 않은 내용을 정상으로 보고
main/master 브랜치에 강제 push
15. Claude Code 연동 팁 (신규)

이 문서를 Claude Code에서 더 안전하게 강제하는 방법이다. 규칙을 텍스트로만 적어두면 에이전트가 실수할 수 있지만, 일부는 시스템 차원에서 막을 수 있다.

15.1 이 문서를 CLAUDE.md로 저장

프로젝트 루트에 CLAUDE.md로 저장하면 Claude Code가 세션 시작 시 자동으로 읽는다. 매번 규칙을 다시 설명할 필요가 없다.

15.2 permission 규칙으로 강제

.claude/settings.json에 다음과 같이 위험한 동작을 아예 차단할 수 있다(예시, 실제 문법은 최신 공식 문서 기준으로 확인 필요).

json
{
  "permissions": {
    "deny": [
      "Read(.env)",
      "Edit(.env)",
      "Bash(git push --force*)",
      "Bash(rm -rf *)"
    ]
  }
}
15.3 Hooks로 검증 자동화

파일 수정 후 자동으로 lint/build를 실행하도록 Hook을 걸어두면 6장의 "문법 검사"를 사람이 매번 챙기지 않아도 된다. (PostToolUse Hook에서 npm run lint 실행 등)

15.4 서브에이전트로 분리

"배포 ZIP 작성"이나 "승인 문서 보호 검증" 같은 반복 작업은 .claude/agents/에 전용 Subagent로 만들어두면 매번 전체 컨텍스트를 설명하지 않고 /agent 이름으로 바로 실행할 수 있다.